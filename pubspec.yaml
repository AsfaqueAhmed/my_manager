name: my_manager
version: 1.0.0+1
publish_to: none
description: A new Flutter project.
environment:
  sdk: '>=2.17.6 <3.0.0'

dependencies:

  flutter:
    sdk: flutter

  cupertino_icons: any
  get: any
  get_storage:
  firebase_core:
  cloud_firestore:

  google_fonts:

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/bottom_bar_icons/
    - assets/gif/

